1. ➜  scale-bridge git:(main) ✗ ./target/release/scale-bridge serve --port 8443 --scale-port /dev/ttyUSB0 --cert cert.pem --key key.pem
error: transport error: specify --serial-port (serial) or --host (TCP/Ethernet)

Error message should say specify --serial-port (serial); differentiate the HTTPS port to --https-port instead of --port

2. add author (ye + email)

3. add license (GPL or MIT?)

4. evaluate the serial comm port with USB adaptor and how does it work. Test on real physical hardware.

5. build windows binary and Test

6. build macOS binary and Test

7. remove SCP-01.pdf maybe license restriction. Need to review legal on publishing it on MIT license open source

8. --serial-port /dev/ttyUSB0 --baud 9600 default those if not provided in command line. Also print it out to notify. also having -v off to disable / quiet it.
